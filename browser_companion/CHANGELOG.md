# Changelog

## Unreleased

## 0.2.1

- Add-on store icon and logo
- Config-flow companion link opens the sidebar panel at `/{slug}` instead of `/hassio/ingress/{slug}` and `my.home-assistant.io`
- Custom components should use the `ha-browser-companion` PyPI package 0.1.1 (`python/` in this repository)

## 0.2.0

- English README and add-on store docs, including the full session API
- GitHub Actions CI that runs pytest
- Store / add-on URL set to the public GitHub repository

## 0.1.6

- Session API, Chromium Ingress panel, and wait-rule capture (unpublished baseline)
